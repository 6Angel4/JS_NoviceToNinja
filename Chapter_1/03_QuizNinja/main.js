alert("Welcome to Quiz Ninja!"); // added Chapter 1
// New adds chapter 2
const question = "What is Superman's real name";
const answer = prompt(question);
alert(`You answered ${answer}`);

/**
 * 1- Se declaro una variable de nombre question y se le asigno como valor un String
 * 2- Se creo una variable de nombre answer, que contiene un cuadro de dialogo que permite
 * al usuario responder, la respuesta se almacena en la variable answer
 * 3- Finalmente se muestra una alerta que contiene el valor de la variable answer
 */
// New adds chapter 2
