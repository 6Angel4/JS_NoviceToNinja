/**
 * Si una variable tiene asignado un valor numerico
 * este puede ser incrementado usando la siguiente operacion
 */
let points = 0;
points = points + 10;
// Forma mas corta para hacer lo anterior
points += 10;
points -= 5;
points *= 2;
points /= 3;
points %= 7;
