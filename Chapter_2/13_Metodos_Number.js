/**
 * Los numeros tambien tienen algunos metodos incorporados
 * aunque se debe de tener cuidado con estos, pues se
 * pueden interpretar como numeros decimales
 */

//Ejemplos de como utilizar metodos en Numeros
console.log((5).toExponential());
console.log((5).toExponential());
console.log((5.0).toExponential());
console.log((5).toExponential());
const PI = 3.141592;
console.log(PI);
console.log(typeofPI.toFixed(3)); //Si solo queremos que redondee hasta 3 decimales, pero el valor es retornado como String

